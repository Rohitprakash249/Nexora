Here's the final code of the assignment.
Note:- i created this assignment in 6-7 hours since i saw the message late.

instructions to run :-

A. front-end
steps :- 
1.clone this repository by running this code in your email "git clone https://github.com/Rohitprakash249/Nexora.git"
2. navigate to "nexora-frontend" folder then run "npm install"
3. run "npm run dev" after installing all the dependencies.

B.Back-end:- 
1. navigate to "nexora-backend" folder then run "npm install"
2. run "node server.js" after installing all the dependencies.

thanks.
